definitions:
  Boom:
    properties:
      error:
        type: string
      message:
        type: string
      statusCode:
        type: integer
  Swagger:
    properties:
      swagger:
        type: string
      info:
        type: object
        properties:
          title:
            type: string
          description:
            type: string
          version:
            type: string
      host:
        type: string
      schemes:
        type: array
        items: string
      basePath:
        type: string
      securityDefinitons:
        type: object
      produces:
        type: array
        items: string
      paths:
        type: object
      responses:
        type: object
      definitions:
        type: object
